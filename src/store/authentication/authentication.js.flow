// @flow
export type AuthenticationState = {
  +email: string,
  +token?: string,
  +tokenId?: string,
  +expireAt?: number,
  +canSendCode: boolean,
  +isAuthenticated: boolean,
  +isCodeReceived: boolean
};
