// @flow

export type AdminUIState = {
  +isMobileViewport: boolean,
  +currentTabIndex: number,
  +dialogContent?: any,
  +currentOpenDialog: string,
  +currentTabId: string,
  +isMenuOpen: boolean
};
